
1. Класс "Bot" (Бот):
   - Метод "приветствие", который приветствует пользователя и объясняет, что делает бот и за что отвечают кнопки в главном меню.
   - Метод "главное_меню", который отображает главное меню и ожидает выбора пользователя. Возвращает выбранный пункт меню.

2. Класс "Опросник":
   - Метод "создать", который создает анкету для опроса пациента о его жалобах.
   - Метод "заполнить", который позволяет пользователю заполнить анкету по вопросам.
   - Метод "получить_aнамнез", который возвращает заполненную анкету пациента.

3. Класс "Выписки":
   - Метод "выбрать_фото", который позволяет пользователю прикрепить фотографии предыдущих выписок.

4. Класс "Помощь":
   - Метод "показать_инструкцию", который отображает инструкцию по заполнению документов и т.д.

user_data = await state.get_data()
permissibleСountValues = sum(len(str(value))for value in user_data.values())

user_data = json.dumps(user_data, indent=4, ensure_ascii=False)

correctKeys = Keys(user_data)
user_data = correctKeys.changingKeys()

permissibleСount = json.dumps(user_data, indent=4, ensure_ascii=False)
print(len(permissibleСount)+permissibleСountValues)

